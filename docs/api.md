# API

Base path: `/api/v1`. Swagger: `/api/docs`. Success responses contain `data` and `requestId`; failures contain a Vietnamese message, stable code and requestId. List endpoints accept page, pageSize, branchId, status, from and to. Core handover endpoints implement create, submit, confirm, request supplement, resubmit, history and participant history.
