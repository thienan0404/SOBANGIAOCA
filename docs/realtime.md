# Realtime

Realtime is limited to handovers, pending tasks, notifications and shift instances. Channels are scoped by organization and branch. The web client invalidates TanStack Query caches on events and unsubscribes on unmount.
