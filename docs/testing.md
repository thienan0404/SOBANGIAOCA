# Testing

`pnpm test` runs workspace unit tests. `pnpm test:e2e` runs Playwright. `pnpm lint`, `pnpm typecheck` and `pnpm build` are required before merge. Integration tests require test DATABASE_URL and Supabase configuration.
