import {HandoverStatus} from '@prisma/client';
export function canTransition(from:HandoverStatus,to:HandoverStatus){const allowed:Record<HandoverStatus,HandoverStatus[]>={DRAFT:[HandoverStatus.SUBMITTED,HandoverStatus.CANCELLED],SUBMITTED:[HandoverStatus.PENDING_RECEIVER_CONFIRMATION],PENDING_RECEIVER_CONFIRMATION:[HandoverStatus.CONFIRMED,HandoverStatus.SUPPLEMENT_REQUESTED],SUPPLEMENT_REQUESTED:[HandoverStatus.RESUBMITTED],RESUBMITTED:[HandoverStatus.PENDING_RECEIVER_CONFIRMATION],CONFIRMED:[HandoverStatus.COMPLETED],COMPLETED:[],CANCELLED:[],OVERDUE:[HandoverStatus.CONFIRMED,HandoverStatus.SUPPLEMENT_REQUESTED]};return allowed[from].includes(to)}
