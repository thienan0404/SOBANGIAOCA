'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';export default function Forgot(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');return <main className="auth-screen"><form className="auth-card" onSubmit={async e=>{e.preventDefault();await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setMessage('Đã gửi hướng dẫn đặt lại mật khẩu')}}><h1>Quên mật khẩu</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="login-button">Gửi hướng dẫn</button>{message&&<p>{message}</p>}</form></main>}
