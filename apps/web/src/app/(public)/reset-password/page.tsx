'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';export default function Reset(){const[password,setPassword]=useState('');const[message,setMessage]=useState('');return <main className="auth-screen"><form className="auth-card" onSubmit={async e=>{e.preventDefault();const{error}=await createClient().auth.updateUser({password});setMessage(error?error.message:'Đã đổi mật khẩu')}}><h1>Đặt lại mật khẩu</h1><label>Mật khẩu mới<input type="password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="login-button">Lưu mật khẩu</button>{message&&<p>{message}</p>}</form></main>}
