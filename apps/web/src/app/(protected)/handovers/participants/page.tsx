'use client';import {useQuery} from '@tanstack/react-query';import {handoverApi} from '@/features/handovers';export default function Participants(){const{data,isLoading}=useQuery({queryKey:['participants'],queryFn:handoverApi.participants});return <><h1>Người giao → người nhận</h1>{isLoading?<p>Đang tải...</p>:<div className="card-list">{data?.map(p=><article className="handover-card blue" key={p.id}><div className="card-icon">⇄</div><div className="card-body"><h3>{p.handover.code}</h3><p>{p.participantType==='GIVER'?'Người giao':'Người nhận'}: {p.user.fullName}</p></div></article>)}</div>}</>}
